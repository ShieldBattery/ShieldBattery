syntax = "proto3";

package shieldbattery.proto.messages;

// TODO(tec27): Write a version that deals with acks and can include multiple storm messages
message StormWrapper {
  bytes storm_data = 1;
}
