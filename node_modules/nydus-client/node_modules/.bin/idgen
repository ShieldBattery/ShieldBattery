#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../idgen/bin/idgen.sh" "$@"
  ret=$?
else 
  node  "$basedir/../idgen/bin/idgen.sh" "$@"
  ret=$?
fi
exit $ret
