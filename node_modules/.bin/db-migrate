#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../db-migrate/bin/db-migrate" "$@"
  ret=$?
else 
  node  "$basedir/../db-migrate/bin/db-migrate" "$@"
  ret=$?
fi
exit $ret
