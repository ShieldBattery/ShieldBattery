This code is common to all the pieces of the app (`app`, `client`, `game`, and `server`).

It has to be in here to it can be properly distributed inside the standalone app's ASAR (without a
build step, anyway) =/
