/**
 * The number of games a player must player (in a single matchmaking type) to not be considered
 * 'new' any more (and have their rating change rate adjusted downward as a result).
 */
export const NEW_PLAYER_GAME_COUNT = 25
