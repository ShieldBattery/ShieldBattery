This directory should contain the latest installer files generated by the build process.

Copy the latest installer exe to a file named `ShieldBattery.latest.exe` to keep it linked from all
the relevant pages.
