/** Maximum image size that we allow to be uploaded. */
export const MAX_IMAGE_SIZE_BYTES = 5 * 1000 * 1000 // 5MB

/** The width/height (in pixels) that uploaded user avatars are resized to (they are square). */
export const USER_AVATAR_SIZE = 256
