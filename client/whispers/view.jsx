import React from 'react'
import ContentLayout from '../content/content-layout.jsx'

export default props => (<ContentLayout title={`Whisper from ${props.routeParams.user}`}>
  <span>TODO {__filename}</span>
</ContentLayout>)
